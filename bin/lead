#!/usr/bin/env coffee
[_,_,path] = process.argv
CSON = require "cson"
parse = CSON.parseSync

{read,log,abort} = require "fairmont"

Lead = require "../lead"
try
  options = parse read path
catch error
  abort error

lead = new Lead options
lead.run()