[Unit]
Description=Spin Up Elasticsearch
After=skydns.service
Requires=docker.service

[Service]
TimeoutStartSec=0
ExecStart=/usr/bin/docker run --name elasticsearch dockerfile/elasticsearch

[Install]
WantedBy=multi-user.target
