[Unit]
Description=Spin Up Redis
After=skydns.service
Requires=docker.service

[Service]
TimeoutStartSec=0
ExecStart=/usr/bin/docker run --name redis dockerfile/redis

[Install]
WantedBy=multi-user.target
