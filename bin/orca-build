#!/usr/bin/env coffee

[_, _, configuration,destination] = process.argv
build = require "../src/build"

if configuration? and destination? 
  build configuration, destination
else
  process.stderr.write "Usage: orca-build <configuration-file> <destination-directory>\n"
  process.exit -1


